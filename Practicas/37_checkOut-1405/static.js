//hago NombredeClase.metodo porque el metodo es static
//en vez de crear un objeto.metodo o objeto.metodo
//es conveniente no mezclar 

let descuento = CheckOut.calcularDescuento(1000,20);

let recargo = CheckOut.calcularRecargo(1000,parseInt(cuotas,5))